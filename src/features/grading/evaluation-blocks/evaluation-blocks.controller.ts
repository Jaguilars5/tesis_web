import { useAppDispatch, useAppSelector } from "@shared/redux/hooks"; import { useCallback, useState } from "react"; import { evaluationBlockService } from "./evaluation-blocks.service"; import { loadPending, loadSuccess, loadError, entityCreated, entityUpdated, entityDeleted, mutationError, selectEvaluationBlocks, selectEvaluationBlocksStatus, selectEvaluationBlocksError } from "./evaluation-blocks.slice"; import type { EvaluationBlockCreateDataT, EvaluationBlockCreateParamsT, EvaluationBlockDeleteParamsT, EvaluationBlockFormValues, EvaluationBlockListParamsT, EvaluationBlockT, EvaluationBlockUpdateParamsT } from "./evaluation-blocks.types";
export type CreateRejectValue = { msg: string; data: Record<string, string> | null; }; export type ValidationErrors = Record<string, string>; export interface SubmitErrorState { general: string[]; validation: ValidationErrors; }
const toRejectValue = (e: unknown): CreateRejectValue => { const msg = e instanceof Error ? e.message : "Error desconocido"; const cause = e instanceof Error ? (e as { cause?: unknown }).cause : undefined; const d = (cause as { response?: { data?: { data?: unknown } } })?.response?.data; return { msg, data: (d?.data as Record<string, string> | null) ?? null }; };
export const useEvaluationBlockController = () => {
  const dispatch = useAppDispatch(); const evaluationBlocks = useAppSelector(selectEvaluationBlocks); const status = useAppSelector(selectEvaluationBlocksStatus); const error = useAppSelector(selectEvaluationBlocksError);
  const loadEvaluationBlocks = useCallback(async (p?: EvaluationBlockListParamsT) => { dispatch(loadPending()); try { const items = await evaluationBlockService.list(p ?? { page: 1, pageSize: 100 }); dispatch(loadSuccess(items)); } catch (err) { dispatch(loadError(err instanceof Error ? err.message : "Error")); } }, [dispatch]);
  const create = useCallback(async (d: EvaluationBlockCreateParamsT): Promise<EvaluationBlockT> => { try { const c = await evaluationBlockService.create(d); dispatch(entityCreated(c)); return c; } catch (err) { const rv = toRejectValue(err); dispatch(mutationError(rv.msg)); throw rv; } }, [dispatch]);
  const update = useCallback(async (p: EvaluationBlockUpdateParamsT): Promise<EvaluationBlockT> => { try { const u = await evaluationBlockService.update(p); dispatch(entityUpdated(u)); return u; } catch (err) { const rv = toRejectValue(err); dispatch(mutationError(rv.msg)); throw rv; } }, [dispatch]);
  const remove = useCallback(async (id: EvaluationBlockDeleteParamsT): Promise<void> => { try { const { id: deletedId } = await evaluationBlockService.softDelete(id); dispatch(entityDeleted(deletedId)); } catch (err) { dispatch(mutationError(err instanceof Error ? err.message : "Error")); } }, [dispatch]);
  return { evaluationBlocks, isLoading: status === "loading", error, loadEvaluationBlocks, createEvaluationBlock: create, updateEvaluationBlock: update, deleteEvaluationBlock: remove };
};
export type EvaluationBlockControllerT = ReturnType<typeof useEvaluationBlockController>;
const extractError = (e: unknown) => { if (e && typeof e === "object" && "msg" in e && typeof (e as { msg?: unknown }).msg === "string") { const obj = e as CreateRejectValue; const fieldErrors: ValidationErrors = {}; const general: string[] = []; if (obj.data && typeof obj.data === "object") { for (const [key, value] of Object.entries(obj.data)) { if (typeof value === "string") fieldErrors[key] = value; else general.push(`${key}: ${value}`); } } if (general.length === 0 && Object.keys(fieldErrors).length === 0) general.push(obj.msg); return { msg: obj.msg, general, validation: fieldErrors }; } const msg = e instanceof Error ? e.message : "Error desconocido"; return { msg, general: [msg], validation: {} }; };
const unwrapCreate = async (d: EvaluationBlockCreateDataT, create: EvaluationBlockControllerT["createEvaluationBlock"]) => { try { return { ok: true as const, result: await create(d), errors: { general: [], validation: {} } as SubmitErrorState }; } catch (e) { const p = extractError(e); return { ok: false as const, result: null, errors: { general: p.general, validation: p.validation } as SubmitErrorState }; } };
const unwrapUpdate = async (p: EvaluationBlockUpdateParamsT, update: EvaluationBlockControllerT["updateEvaluationBlock"]) => { try { return { ok: true as const, result: await update(p), errors: { general: [], validation: {} } as SubmitErrorState }; } catch (e) { const parsed = extractError(e); return { ok: false as const, result: null, errors: { general: parsed.general, validation: parsed.validation } as SubmitErrorState }; } };
interface UseFormArgs { create: EvaluationBlockControllerT["createEvaluationBlock"]; update: EvaluationBlockControllerT["updateEvaluationBlock"]; }
export const useEvaluationBlockForm = ({ create, update }: UseFormArgs) => {
  const [isOpen, setIsOpen] = useState(false); const [editingEvaluationBlock, setEditingEvaluationBlock] = useState<EvaluationBlockT | null>(null); const [submitErrors, setSubmitErrors] = useState<SubmitErrorState>({ general: [], validation: {} }); const isEdit = !!editingEvaluationBlock;
  const openModal = useCallback((eb?: EvaluationBlockT) => { setEditingEvaluationBlock(eb ?? null); setSubmitErrors({ general: [], validation: {} }); setIsOpen(true); }, []);
  const closeModal = useCallback(() => { setIsOpen(false); setEditingEvaluationBlock(null); setSubmitErrors({ general: [], validation: {} }); }, []);
  const handleSubmit = useCallback(async (values: EvaluationBlockFormValues) => { setSubmitErrors({ general: [], validation: {} }); if (editingEvaluationBlock) { const result = await unwrapUpdate({ id: editingEvaluationBlock.id, data: values }, update); if (result.ok) { closeModal(); return; } setSubmitErrors(result.errors); } else { const { code, name, weight_percentage, academic_period, tipo } = values; const result = await unwrapCreate({ code, name, weight_percentage, academic_period, tipo }, create); if (result.ok) { closeModal(); return; } setSubmitErrors(result.errors); } }, [editingEvaluationBlock, create, update, closeModal]);
  return { isOpen, isEdit, editingEvaluationBlock, submitErrors, openModal, closeModal, handleSubmit };
};
