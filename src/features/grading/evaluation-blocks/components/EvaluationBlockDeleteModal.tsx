import { AlertTriangle, Loader2, X } from "lucide-react"; import { useState } from "react"; import type { EvaluationBlockT } from "../evaluation-blocks.types";
export const EvaluationBlockDeleteModal = ({ isOpen, evaluationBlock, onClose, onConfirm }: { isOpen: boolean; evaluationBlock: EvaluationBlockT | null; onClose: () => void; onConfirm: (id: number) => Promise<void>; }) => {
  const [isDeleting, setIsDeleting] = useState(false); if (!isOpen || !evaluationBlock) return null;
  const h = async () => { setIsDeleting(true); try { await onConfirm(evaluationBlock.id); onClose(); } finally { setIsDeleting(false); } };
  return (<div className="fixed inset-0 z-50 flex items-center justify-center p-4"><div className="absolute inset-0 bg-black/40" onClick={onClose} /><div className="relative w-full max-w-sm animate-slide-up overflow-hidden rounded-xl bg-white shadow-xl"><div className="flex items-center justify-between border-b border-slate-200 px-5 py-4"><h3 className="text-lg font-semibold text-slate-900">Desactivar Bloque</h3><button type="button" onClick={onClose} disabled={isDeleting} className="rounded-lg p-1.5 text-slate-400 hover:bg-slate-100"><X className="size-5" /></button></div><div className="p-5"><div className="flex flex-col items-center gap-4 py-2 text-center"><span className="flex size-12 items-center justify-center rounded-full bg-red-50 text-red-500"><AlertTriangle className="size-6" /></span><div><p className="text-sm text-slate-600">¿Desactivar <span className="font-semibold text-slate-900">{evaluationBlock.name}</span>?</p>{evaluationBlock.code && <p className="text-xs text-slate-500">{evaluationBlock.code}</p>}</div></div></div><div className="flex items-center justify-end gap-3 border-t border-slate-200 px-5 py-4"><button type="button" onClick={onClose} disabled={isDeleting} className="rounded-lg border border-slate-300 bg-white px-4 py-2 text-sm font-medium text-slate-700 hover:bg-slate-50">Cancelar</button><button type="button" onClick={h} disabled={isDeleting} className="inline-flex items-center gap-2 rounded-lg bg-red-600 px-4 py-2 text-sm font-medium text-white hover:bg-red-700">{isDeleting ? <><Loader2 className="size-4 animate-spin" />Desactivando...</> : "Desactivar"}</button></div></div></div>);
};
