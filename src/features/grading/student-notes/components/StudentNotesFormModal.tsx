import { useFormik } from "formik"; import { useEffect } from "react"; import { inputClassname, selectClassname } from "@app/styles/styles"; import { CustomInput, CustomSelect } from "@shared/components/Form"; import { studentNoteSchema } from "../student-notes.utils"; import type { SubmitErrorState } from "../student-notes.controller"; import type { StudentNoteFormValues, StudentNoteT } from "../student-notes.types";
const getFieldLabel = (f: string): string => ({ enrollment: "Matrícula", evaluative_activity: "Actividad", grading_mode: "Tipo", numeric_score: "Puntaje", qualitative_scale: "Escala", teacher_observation: "Observación", non_field_errors: "Error general" }[f] || f);
interface Props { isOpen: boolean; onClose: () => void; isEdit: boolean; editingStudentNote: StudentNoteT | null; onSubmit: (values: StudentNoteFormValues) => Promise<void>; submitErrors: SubmitErrorState; enrollmentOptions: { label: string; value: string }[]; evaluativeActivityOptions: { label: string; value: string }[]; gradeTypeOptions: { label: string; value: string }[]; qualitativeScaleOptions: { label: string; value: string }[]; }
export const StudentNotesFormModal = ({ isOpen, onClose, isEdit, editingStudentNote, onSubmit, submitErrors, enrollmentOptions, evaluativeActivityOptions, gradeTypeOptions, qualitativeScaleOptions }: Props) => {
  const getIV = (): StudentNoteFormValues => { if (editingStudentNote) return { enrollment: editingStudentNote.enrollment ?? "", evaluative_activity: editingStudentNote.evaluative_activity ?? "", grading_mode: editingStudentNote.grading_mode, numeric_score: editingStudentNote.numeric_score ?? null, qualitative_scale: editingStudentNote.qualitative_scale ?? "", teacher_observation: editingStudentNote.teacher_observation ?? "" }; return { enrollment: "", evaluative_activity: "", grading_mode: "", numeric_score: null, qualitative_scale: "", teacher_observation: "" }; };
  const formik = useFormik<StudentNoteFormValues>({ initialValues: getIV(), validationSchema: studentNoteSchema, enableReinitialize: true, onSubmit });
  useEffect(() => { if (isOpen && editingStudentNote) formik.setValues(getIV()); }, [isOpen, editingStudentNote]);
  if (!isOpen) return null;
  return (<div className="fixed inset-0 z-50 flex items-center justify-center"><div className="absolute inset-0 bg-black/40" onClick={onClose} /><div className="relative w-full max-w-lg animate-slide-up overflow-hidden rounded-xl bg-white shadow-xl"><div className="flex items-center justify-between border-b border-slate-200 px-5 py-4"><div><h3 className="text-lg font-semibold text-slate-900">{isEdit ? "Editar" : "Nueva"} Nota</h3><p className="mt-0.5 text-sm text-slate-500">{isEdit ? "Modifique los datos de la nota" : "Registre una nueva nota académica"}</p></div><button type="button" onClick={onClose} className="rounded-lg p-1.5 text-slate-400 hover:bg-slate-100"><svg className="size-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M6 18L18 6M6 6l12 12" /></svg></button></div>
    {(submitErrors.general.length > 0 || Object.keys(submitErrors.validation).length > 0) && (<div className="mx-5 mt-3 rounded-lg border border-red-300 bg-red-50 p-4 shadow-sm"><div className="flex items-start gap-2"><svg className="mt-0.5 size-5 flex-shrink-0 text-red-600" fill="currentColor" viewBox="0 0 20 20"><path fillRule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clipRule="evenodd" /></svg><div className="flex-1"><p className="mb-2 text-sm font-semibold text-red-800">Error al guardar</p>{submitErrors.general.length > 0 && (<ul>{submitErrors.general.map((e, i) => (<li key={i} className="text-sm text-red-700">• {e}</li>))}</ul>)}{Object.keys(submitErrors.validation).length > 0 && (<ul>{Object.entries(submitErrors.validation).map(([f, m]) => (<li key={f} className="text-sm text-red-700"><span className="font-semibold">{getFieldLabel(f)}:</span> {m}</li>))}</ul>)}</div></div></div>)}
    <form onSubmit={formik.handleSubmit} className="space-y-4 p-5">{isEdit && editingStudentNote && (<div className="rounded-lg bg-slate-50 p-3 text-sm text-slate-600"><p><strong>Matrícula:</strong> {editingStudentNote.enrollment_name}</p><p><strong>Actividad:</strong> {editingStudentNote.evaluative_activity_title}</p></div>)}{!isEdit && (<><CustomSelect label="Matrícula" name="enrollment" options={enrollmentOptions} value={formik.values.enrollment} onChange={(o) => formik.setFieldValue("enrollment", o.value)} onBlur={formik.handleBlur} error={formik.touched.enrollment ? formik.errors.enrollment : undefined} className={selectClassname} placeholder="Seleccione" /><CustomSelect label="Actividad" name="evaluative_activity" options={evaluativeActivityOptions} value={formik.values.evaluative_activity} onChange={(o) => formik.setFieldValue("evaluative_activity", o.value)} onBlur={formik.handleBlur} error={formik.touched.evaluative_activity ? formik.errors.evaluative_activity : undefined} className={selectClassname} placeholder="Seleccione" /></>)}<CustomSelect label="Tipo" name="grading_mode" options={gradeTypeOptions} value={formik.values.grading_mode} onChange={(o) => formik.setFieldValue("grading_mode", o.value)} onBlur={formik.handleBlur} error={formik.touched.grading_mode ? formik.errors.grading_mode : undefined} className={selectClassname} placeholder="Seleccione" /><CustomInput label="Puntaje (0-10)" name="numeric_score" value={formik.values.numeric_score ?? ""} onBlur={formik.handleBlur} onChange={(e) => formik.setFieldValue("numeric_score", e.target.value === "" ? null : Number(e.target.value))} type="number" min={0} max={10} error={formik.touched.numeric_score ? formik.errors.numeric_score : undefined} className={inputClassname} /><CustomSelect label="Escala Cualitativa" name="qualitative_scale" options={qualitativeScaleOptions} value={formik.values.qualitative_scale} onChange={(o) => formik.setFieldValue("qualitative_scale", o.value)} onBlur={formik.handleBlur} error={formik.touched.qualitative_scale ? formik.errors.qualitative_scale : undefined} className={selectClassname} placeholder="Seleccione" /><div className="flex items-center justify-end gap-3 border-t border-slate-200 pt-4"><button type="button" onClick={onClose} className="rounded-lg border border-slate-300 bg-white px-4 py-2 text-sm font-medium text-slate-700 hover:bg-slate-50">Cancelar</button><button type="submit" disabled={formik.isSubmitting} className="rounded-lg bg-primary px-4 py-2 text-sm font-medium text-white hover:bg-primary-hover disabled:opacity-60">{formik.isSubmitting ? "Guardando..." : "Guardar"}</button></div></form></div></div>);
};
